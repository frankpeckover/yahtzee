This is just a simple score sheet for yahtzee buillt with Node, React and Mongoose. You need to download the files and install all node packages with node install. Run server and connect with a client